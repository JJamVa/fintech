import React from 'react';
import styled from "styled-components";

const Button = styled.button`
    color: grey;
`;

const StyledComponents = () => {
    return (
        <div>
            <Button>스타일드 컴포넌트</Button>
        </div>
    )
};

export default StyledComponents